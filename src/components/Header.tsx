import React from "react";

const Header: React.FC = () => {
  return (
    <header style={{ padding: "1rem", background: "#282c34", color: "#fff" }}>
      <h1>Todolist Pages</h1>
    </header>
  );
};

export default Header;
